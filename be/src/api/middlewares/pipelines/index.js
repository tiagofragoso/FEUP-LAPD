const search_pipeline = require("./search");
const track_pipeline = require("./track");
const album_pipeline = require("./album");

module.exports = {
    search_pipeline,
    track_pipeline,
    album_pipeline,
};
